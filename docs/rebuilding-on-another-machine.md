# Rebuilding on another machine

Start from the project delivery index and selected delivery manifest. Use the rebuild execution spec, discover host architecture/tools, resolve parameters, obtain required secrets through approved channels, execute stages in order and require all verification gates before completion.
