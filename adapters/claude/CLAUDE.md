# AI Delivery Standard
For closeout/rebuild tasks, read `skill/ai-delivery-closeout/SKILL.md`. Treat the current Claude session as one source among potentially many project sources. Follow JSON contracts and FAIL_CLOSED completion.
